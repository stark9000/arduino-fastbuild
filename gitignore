# Personal, machine-specific settings - regenerated automatically on next
# run, and contain absolute paths specific to whoever's running the app
# (arduino-cli location, sketch paths, etc.) - never meant to be shared.
fastbuild-ui-settings.json
fastbuild-ui-project-settings.json
fastbuild-ui-cache.json

# NetBeans build output - regenerated by Build/Clean and Build
/build/
/dist/

# NetBeans' own private project cache - contains absolute paths specific
# to whoever opened the project, not something to share either
nbproject/private/

# Compiled class files, in case anything ever gets compiled outside build/
*.class

# Editor/OS cruft
.DS_Store
Thumbs.db
*~
*.swp
